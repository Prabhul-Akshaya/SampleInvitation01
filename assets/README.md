# assets/

Place your optional background-music file here, named `music.mp3`
(or update the path in `wedding.json` → `music.file` if you name it
something else).

This folder ships empty on purpose — music choice is personal, and
audio files are large. See the main `README.md` → "Turn background
music on or off" for full instructions.
